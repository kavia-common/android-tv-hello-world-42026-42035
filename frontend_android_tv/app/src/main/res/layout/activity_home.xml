<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingLeft="48dp"
    android:paddingRight="48dp"
    android:paddingTop="48dp"
    android:paddingBottom="48dp"
    android:background="@drawable/bg_subtle_gradient">

    <!-- Top menu row -->
    <LinearLayout
        android:id="@+id/top_menu"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        android:weightSum="4">

        <TextView
            android:id="@+id/menu_home"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="@string/menu_home"
            android:textSize="24sp"
            android:textColor="@color/op_text"
            android:padding="12dp"
            android:focusable="true"
            android:focusableInTouchMode="true"
            android:background="@drawable/focusable_text_background"/>

        <TextView
            android:id="@+id/menu_login"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="@string/menu_login"
            android:textSize="24sp"
            android:textColor="@color/op_text"
            android:padding="12dp"
            android:focusable="true"
            android:focusableInTouchMode="true"
            android:background="@drawable/focusable_text_background"/>

        <TextView
            android:id="@+id/menu_settings"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="@string/menu_settings"
            android:textSize="24sp"
            android:textColor="@color/op_text"
            android:padding="12dp"
            android:focusable="true"
            android:focusableInTouchMode="true"
            android:background="@drawable/focusable_text_background"/>

        <TextView
            android:id="@+id/menu_my_plan"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="@string/menu_my_plan"
            android:textSize="24sp"
            android:textColor="@color/op_text"
            android:padding="12dp"
            android:focusable="true"
            android:focusableInTouchMode="true"
            android:background="@drawable/focusable_text_background"/>
    </LinearLayout>

    <!-- Banner area -->
    <LinearLayout
        android:id="@+id/banner_container"
        android:layout_width="0dp"
        android:layout_height="200dp"
        android:orientation="vertical"
        android:paddingTop="16dp"
        android:paddingBottom="16dp"
        app:layout_constraintTop_toBottomOf="@id/top_menu"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        android:background="@drawable/placeholder_banner">

        <TextView
            android:id="@+id/banner_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/home_banner_title"
            android:textColor="@android:color/white"
            android:textSize="28sp"
            android:paddingLeft="16dp"
            android:paddingTop="12dp"
            android:focusable="false"/>

        <TextView
            android:id="@+id/banner_subtitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/home_banner_subtitle"
            android:textColor="#E5E7EB"
            android:textSize="18sp"
            android:paddingLeft="16dp"
            android:paddingTop="4dp"
            android:focusable="false"/>
    </LinearLayout>

    <!-- Rails container - use a parent ScrollView with vertical orientation -->
    <ScrollView
        android:id="@+id/rails_scroll"
        android:layout_width="0dp"
        android:layout_height="0dp"
        app:layout_constraintTop_toBottomOf="@id/banner_container"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent">

        <LinearLayout
            android:id="@+id/rails_container"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:paddingTop="12dp"
            android:paddingBottom="12dp">

            <!-- Individual rail block (title + horizontal list) repeated via include -->
            <include layout="@layout/view_rail" />
            <include layout="@layout/view_rail" />
            <include layout="@layout/view_rail" />
            <include layout="@layout/view_rail" />
            <include layout="@layout/view_rail" />
            <include layout="@layout/view_rail" />
        </LinearLayout>
    </ScrollView>

</androidx.constraintlayout.widget.ConstraintLayout>
